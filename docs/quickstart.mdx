---
title: "Quickstart"
description: "Run your first QitOS agent on the Agent façade in under 2 minutes."
---

This guide takes you from a fresh environment to a model-backed agent run in under 2 minutes.

<Note>
Need a provider first? Read [Prerequisites](/prerequisites). It explains how to get an OpenAI-compatible API key and validate your endpoint before running QitOS.
</Note>

## Prerequisites

- Python 3.10 or later
- `pip`
- An OpenAI-compatible API key

## Steps

<Steps>
  <Step title="Install QitOS">
    ```bash
    pip install "qitos[models]"
    ```
  </Step>

  <Step title="Set your provider config">
    ```bash
    export OPENAI_API_KEY="sk-..."
    export OPENAI_BASE_URL="https://api.openai.com/v1"
    export QITOS_MODEL="gpt-4o-mini"
    ```

    <Tip>
      Any OpenAI-compatible endpoint works. Point `OPENAI_BASE_URL` at your provider if you're not using OpenAI directly.
    </Tip>
  </Step>

  <Step title="Run the minimal agent">
    ```bash
    python examples/quickstart/minimal_agent.py
    ```

    The example composes the minimal agent loop directly: one `Agent` façade, one
    `OpenAICompatibleModel`, and one `echo` Tool in a `ToolRegistry`. `agent.prompt(...)`
    drives the `Message -> Model -> ToolCall -> ToolResult` loop and returns a typed
    `AgentLoopResult`; the example prints the final assistant message.
  </Step>

  <Step title="Read the composition">
    Open [`examples/quickstart/minimal_agent.py`](https://github.com/Qitor/qitos/blob/main/examples/quickstart/minimal_agent.py):

    ```python
    agent = Agent(
        model=model,
        tool_registry=ToolRegistry().register(echo),
        system_prompt="You are a minimal helpful agent. Answer concisely.",
        max_turns=4,
    )
    result = await agent.prompt("Call the echo tool once, then reply with what it returned.")
    ```

    Everything is explicit: the model, the Tool surface, the prompt, and the turn budget.
    Child agents, journaling, and supervision compose from the same façade — see the
    [kit reference](/reference/kit).
  </Step>
</Steps>

## What's next

<CardGroup cols={2}>
  <Card title="Kit reference" icon="toolbox" href="/reference/kit">
    Tools, child agents, journals, memory, and environment building blocks
  </Card>
  <Card title="Observability" icon="chart-line" href="/guides/observability">
    Everything qita can do: board, replay, and export
  </Card>
</CardGroup>
