---
title: "CLI Reference"
description: "Reference for qita and qit, including the official benchmark CLI added in QitOS v0.3."
---

QitOS ships two top-level CLIs:

- `qita` for trace inspection
- `qit` for benchmarks and developer workflows

## qita

Use `qita` when you want to inspect traced runs.

### `qita board`

```bash
qita board --logdir ./runs
```

Open the multi-run board. It supports:

- run list and filtering
- compare pickers
- run detail links
- replay links
- raw and HTML export

### `qita replay`

```bash
qita replay --run ./runs/<run_id>
```

Open one run in temporal playback mode.

### `qita export`

```bash
qita export --run ./runs/<run_id> --html ./report.html
```

Export a standalone HTML artifact.

## qit bench

`qit bench` handles normalized benchmark result files and run artifacts.

### `qit bench eval`

```bash
qit bench eval --input ./results/tau.jsonl --json
```

Aggregates normalized benchmark results.

### `qit bench replay`

```bash
qit bench replay --run ./runs/<run_id>
```

Bridges to the qita replay surface for one benchmark run.

### `qit bench export`

```bash
qit bench export --run ./runs/<run_id> --html ./report.html
```

Exports one benchmark run as standalone HTML.

## Recommended workflow

For a first run:

1. `export OPENAI_API_KEY=...`
2. `python examples/quickstart/minimal_agent.py`

For benchmark result files produced outside this repository:

1. `qit bench eval`
2. `qita board`
3. `qit bench replay`
4. `qit bench export`

For deeper background, continue to [Tracing](/concepts/tracing).
