---
title: Session Journal
description: 持久化完整模型与工具事务，并支持恢复和分叉。
---

QitOS 为每个 Run 保存一份 append-only JSONL Journal。启用后，它是最小 agent loop 的
模型事务、工具事务、turn 提交与运行生命周期的 canonical 记录。

每个 Journal 实例都是唯一的存储 owner。`create()` 或 `open()` 会取得该 Run 的操作系统级
writer lease；其他进程若尝试同时写入，会得到 `JournalOwnershipError`，而不是打开
第二个 writer。关闭后的实例永久拒绝修改，但仍可从隔离的内存值 replay 或查询已提交工具事务，
便于诊断。

## Transcript 与 operation 记录

一份 Journal 在同一序列中携带两类记录。Transcript 条目拥有消息内容：
`transcript.message`（user、assistant 与 Tool 结果消息）和 `compaction`（摘要加上
第一个保留的 transcript 引用）。Operation 记录拥有生命周期与副作用边界，并按
record id 引用 transcript 条目——消息内容只出现在恰好一条记录中。
`model.completed` 保留精确的请求审计与 assistant transcript 引用；`tool.terminal`
保留调用与终态 transcript 引用；`step.committed` 是纯提交标记，列出该 turn 的
transcript 与 Tool 终态引用；`run.completed` / `run.interrupted` 携带终态状态、
错误，以及可选的有界资源收敛诊断。携带引用的记录通过 `journal.inherited` 包装
fail closed 解析。

## Loop 事务顺序

loop 的 `JournalTurnTransaction` 按以下顺序记录每次运行：

1. 每个已接受的 prompt 一条 `transcript.message`，然后是列出其 record id 的
   `input.accepted`——在第一个模型副作用之前
2. 每 turn 冻结差异 `model.change` / `thinking.change` / `tools.change`
   （首个 turn 写完整三元组，之后只写差异）
3. assistant 消息的 `transcript.message`，然后是 `model.completed`
4. `tool.started`：一次已准入 ToolCall 的持久化执行许可
5. 执行 Handler（仅明确声明安全时并行），并生成唯一 canonical `ToolResult` 候选
6. 严格按 ToolCall 输入顺序写入每个 Tool 结果消息的 `transcript.message`，然后是
   对应的 `tool.terminal`
7. `step.committed`：该 turn 的提交标记

`tool.started` 是持久化执行许可；写入失败时不能执行 Handler。终态结果只持久化一次，
并且与进入 transcript 的值是同一个深度不可变值。并行 Tool 批次按输入顺序提交结果，
若前一个终态写入失败或结果未知，后续候选不得跨过缺口继续写入；关闭并重放后，恢复逻辑
按输入顺序关闭这些调用，且不重新执行 Handler。每个已准入调用（包括拒绝、超时、取消）
最终都恰好得到一个终态结果。

一次已收敛的运行恰好有一个终态记录：`run.completed` 携带终态状态（`completed`、`failed`、
`max_turns`、`deadline_exceeded`）；被中止的运行改为写入
`run.interrupted`。若配置了 Run 唯一的冻结资源 finalizer，它只接收当前 leg 的稳定
Run id，并在任一终态记录前收敛属于该 Run 的资源。它不得关闭供后续 Session leg 复用的
Env、Tool 或 MCP owner；完整 teardown 由最外层应用恰好执行一次。清理失败只写入类型化
诊断，不覆盖原始状态或错误。外部任务取消与普通故障会在 canonical 写入成功收敛时，先
终结已开始的工作和运行记录再重新抛出。若 append 自身失败或结果未知，writer 不得跨过
未关闭的 Tool 边界猜写 Run 终态；必须先关闭并重放，再由恢复逻辑补齐 Tool 终态。

## 恢复

`recover_session(records)` 将一份 Journal 重放为类型化 transcript、compaction 投影
后的上下文、配置 lineage（模型、thinking 级别、活跃 Tool——包括由早前 Tool 结果激活
的 Tool）、未完成的 Tool 操作、未消费的 runtime 输入以及终态结果。它是对一份 Journal
重放的纯函数——继承的 fork 前缀通过包装解析，因此一份 fork 出的 Journal 自包含完整
恢复真相——并在矛盾处 fail closed 抛出 `JournalCorruptionError`：悬空的 transcript
引用、缺少对应 `tool.terminal` 的 Tool transcript 条目、覆盖未完成调用的 commit 或运行
终态、未知 commit 引用、冲突的运行终态、运行终态之后的记录、turn 回退、非法
compaction 切点，以及旧 Engine 时代的载荷形状。

已准入但从未终结、或从未准入的 Tool 调用，在没有 commit 或运行终态覆盖时是合法的崩溃
窗口。`close_crashed_tool_calls(journal, recovered)` 关闭该窗口而不重新执行任何操作、
不猜测副作用：每个调用得到一条显式 cancelled 的 `tool.terminal` 及其 transcript 条目
（已持久化的撕裂条目会被链接而不是替换），以及一条收尾的 `step.committed`。合成的错误
文本区分"已开始、结果未知"与"从未准入、无副作用"。record id 与载荷是恢复后 Journal
的确定性函数，因此重复关闭不会重复追加。

`InMemorySessionJournal` 在内存中实现同一个 `SessionJournal` 契约——持久化范围限定在
共享的 `InMemoryJournalStore`——并与 JSONL 实现共享同一套一致性测试。已发布 runtime
输入的消费以 `runtime_input.consumed` 写入 journal，因此未消费的输入在恢复后恰好重新
投影一次，而继承的 fork 事实永远不会被重新投递。

## Session Harness

`qitos.kit.session.SessionHarness` 是这些 journal 之上 Session 生命周期的权威所有者。
它以一个 journal 根目录（JSONL journal 目录）或一个共享的 `InMemoryJournalStore`
构造，并可选用 `CompactionSettings` 与 trace 目录。

```python
from qitos.kit.session import SessionHarness

harness = SessionHarness(".qitos/sessions")
session = await harness.start(model=model, tool_registry=registry)
result = await session.prompt("排查预发环境的部署")
await session.close()
```

`resume(run_id, ...)` 原地继续一个未完成的 journal：通过纯恢复重放 journal，用显式
cancelled 终态关闭崩溃窗口（绝不重新执行），把上下文消息、thinking level 和配置谱系
恢复到新的 `Agent` 中，并把每个未消费的 runtime 输入恰好重新投影一次。预期的不匹配
返回类型化的 `ResumeRejected` 而不是抛异常：`not_found`、`terminal`（fork 是显式的
继续方式）、`model_mismatch`（journal 中的 provider/model/api 身份与提供的模型不一致）、
`tools_missing`（journal 谱系中激活的 Tool——包括由更早 Tool 结果激活的——不在提供的
registry 中，会附带缺失名称）以及 `busy`（其他写者持有 lease）。损坏抛出
`JournalCorruptionError`；Tool 对象永远不会从 journal 重建。

`fork(run_id, position=None, ...)` 在一个已提交的边界（默认最新）把 journal 分叉为一个
嵌入继承前缀的、自包含的新 journal。对终态 Run 的 fork 是继续已完成工作的显式方式；
在同一个 `SessionRun` 内，当前 leg 结束后再 prompt 也会沿同一 fork 机制前进，因此每个
journal 都保持只追加，而会话保持可交互。一个 Run journal 只接受一条 `input.accepted`；
向已恢复的 journal 发送新 prompt 会作为 steering 在下一个 turn 前注入。

Compaction 遵循 Pi 的成熟算法：`compact()` 只在空闲时运行（`busy` 与
`nothing_to_compact` 返回类型化拒绝），切点绝不落在 Tool 结果上、也绝不落在 Tool 调用
与其结果之间，切分 turn 时会合并 turn 前缀摘要，迭代 compaction 会把上一次摘要折入新
摘要。摘要由该 run 自己的模型生成（一次有界请求，不暴露 Tool），作为持久的
`compaction` 记录落盘（摘要 usage 可用时一并记录），并通过替换 transcript 投影到
façade——这同时封存 Provider continuation，使下一个请求成为完整请求。配置
`CompactionSettings` 后，harness 还会在空闲边界按模型的 `context_window` 评估 token
阈值，并在一次上下文溢出失败后执行一次"压缩并继续"的恢复尝试。

## 持久化与投影

JSONL writer 每条记录都等待持久化确认。Darwin 使用 `F_FULLFSYNC`；其他 POSIX 系统优先
使用 `fdatasync`。

每次追加都会先把载荷转换为一份隔离的严格 JSON 值：Python 元组变成 JSON 数组；
非字符串键、非有限数值、bytes、集合、循环引用和自定义对象都会在写入前被拒绝。同一个值
因此同时驱动内存 replay、稳定 record id 结算、JSONL 编码与重新打开后的 replay。

`journal.index.sqlite3` 是 canonical 文件旁的一次性读投影。QitOS 只在 JSONL 追加持久化后
更新它。缺失、过期、损坏或版本不符的投影会从校验过的 JSONL 记录重建。打开与 replay 永远
先解析 canonical JSONL；SQLite 不提供 replay 记录。语义无效的完整记录抛出
`JournalCorruptionError`；schema 过旧或过新的有效记录抛出
`JournalUnsupportedVersionError`。两者都 fail closed；只有未终止的尾部撕裂会在恢复时截断。

`JsonlRunCatalog` 是这个边界的只读一侧。它可以在 writer lease 仍活跃时检查或列出 Run，
也可以在不构造 Agent 的情况下校验祖先与直接子代 lineage。catalog 读取使用当前完整 JSONL
前缀；SQLite 只是一次性索引，绝不提供 canonical 记录。`RunStatus.RESUMABLE` 只表示没有
持久化的 `run.completed`。

每个 `RunHandle` 还暴露稳定的 `lineage_id`、直接父级 `parent_run_id`、最新的持久化
`committed_position`、`continuation_position` 和 `has_terminal_state`。

```python
from qitos.kit.journal import JsonlRunCatalog, JsonlSessionJournal

catalog = JsonlRunCatalog(".qitos/sessions")
handle = await catalog.inspect_run(run_id)
all_runs = await catalog.list_runs()
ancestors = await catalog.lineage(run_id)

if handle.is_terminal and handle.continuation_position is not None:
    source = JsonlSessionJournal(".qitos/sessions")
    await source.open(handle.run_id)
    child = await source.fork(handle.continuation_position, "follow-up-run")
    await child.close()
    await source.close()
```

fork 只接受已提交的 `JournalPosition`。子 journal 拥有独立的继承前缀，可以在没有祖先的
情况下恢复；继承来的本地提交本身也是有效边界，因此 fork 出的 Run 可以再次 fork。
每个返回的打开 journal 都由调用方持有，直到调用 `close()`。

## 已提交工具查询

每个 canonical Tool 终态记录在持久化后都可以通过轻量 `JournalRecordRef` 定位：

```python
from qitos.core.journal import JournalRecordRef

reference = JournalRecordRef(run_id=origin_run_id, record_id=terminal_record_id)
transaction = journal.find_tool_transaction(reference)
if transaction is not None:
    result = transaction.result
```

该查询是只读的，只有在终态记录被 `step.committed` 点名后才返回新的类型化
`ToolTransaction`；结果从被引用的 `transcript.message` 记录联接得到，引用缺失或不
匹配时 fail closed。打开 journal 时会从 canonical JSONL 记录重建这份查询视图。fork 通过
父级来源引用解析继承的终态记录，子级不会把结果复制到第二个存储。未知、未提交或被
子级改名的引用返回 `None`。只有 loop 的记录 schema 可以解码；旧 Engine taxonomy 写入的
记录会 fail closed。

## Subagent 与 mailbox 记录

Subagent 监督在构造 Subagent 之前写入 `subagent.started`，在通知父级之前写入
`subagent.terminal`。恢复时，如果 Subagent 自己的 loop journal 中存在持久化运行终态记录，
就从该 journal 重建终态结果，否则将其关闭为 `interrupted`；恢复永远不会重新挂载或重放
Subagent。恢复期间
创建的终态记录会在 supervisor 报告恢复结果之前持久化。fork 保留继承的事实用于审计，
但不会获得对父级 handle 的控制权。

发给活跃 Subagent 的父级消息会先作为 `runtime_input.posted` 写入 journal，再进入其运行的
steering 队列，因此 mailbox 接收可以从 journal 恢复，消息会在 Subagent 下一个 turn 安全点
可见。当被 steering 的消息进入已提交的 transcript 后，Subagent 引擎会追加幂等的
`runtime_input.consumed` 记录；如果运行结束时消息尚未注入，则不产生任何消费记录。
